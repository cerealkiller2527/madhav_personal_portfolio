// Re-export types from the new types directory for backward compatibility
export type {
  BlogPost,
  BlogPostPreview,
  BlogConfig,
  BlogMetadata,
  ValidationResult,
  BlogError,
  BlogErrorCode,
  CacheEntry,
  CacheConfig
} from "@/types/blog"